## Proposal from tester
Run: squad-24d374f4-8ab4-47ec-ac4d-7e6ff854e255

PROPOSAL: 为 generation 0 的 `StartListeningAudio` 和 listening 状态下的 wake-word re-listen 增加 SendStartListening=false 的 close/Idle/wake-word 恢复，不能只 return 或记录 warning。
