## Proposal from backend
Run: squad-4bbd3469-958b-4cf1-8c8a-7e017706037e

Propose keeping CONFIG_STROKE_ORDER_LOCAL off until CoreS3 LVGL pointer routing and StrokeOrderView land together, so the parser is not compiled into unused firmware.
